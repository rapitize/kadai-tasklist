<% if tasks.any? %>
  <ul class="list-unstyled mt-2">
    <% tasks.each do |task| %>
      <li class="d-flex">
        <div>
            <p><%= link_to task.id, task %> : <%= task.status %> &gt; <%= task.content %></p>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>